first standard syntax of a c program .
declare your ints and floats and doubles progressively.
use a for loop for no .of customers.
now show the hotel rooms and prices .
and additional services which would be wifi,gym,breakfast.........
now make or calculate the base fare.
then add a discount of 10% if base fare >10000 by using if and else conditions .
calculate 12% GST and total bill which would be grand total in my cas.
cal total revenue in the first for loop.
then start the program and enter valuses and get the answer.
the end 
